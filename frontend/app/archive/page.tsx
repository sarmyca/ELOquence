import Archive from '@/components/Archive';

export const metadata = {
  title: 'Archive — ELOquence',
  description: 'Replay any past daily Wordle puzzle.',
};

export default function ArchivePage() {
  return <Archive />;
}
